# API
# fastapi
# uvicorn[standard]

# RAG core
# embedchain
chromadb>=0.5
# langchain-community

# Models & uploads
pydantic>=2.7,<3
python-multipart

# PDF text extraction (Embedchain uses this under the hood; ensure it's present)
pypdf>=4

# LLM provider used by Embedchain (set OPENAI_API_KEY in env)
openai>=1.0

# Optional: if you switch to a managed vector DB later, uncomment:
# qdrant-client

embedchain>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0
fastapi>=0.111
uvicorn[standard]>=0.30
PyYAML>=6.0